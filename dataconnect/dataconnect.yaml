specVersion: "v1beta"
serviceId: "jobplace"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jobplace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
